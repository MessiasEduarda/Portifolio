export const theme = {
  colors: {
    primary: '#your-primary-color', // Substitua pela sua cor primária
    white: '#FFFFFF',
    default: '#333333', // Substitua pela cor padrão de texto
    // Adicione outras cores conforme necessário
  },
} as const;

export type Theme = typeof theme;